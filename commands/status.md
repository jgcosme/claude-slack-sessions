---
description: Show the slack-sessions daemon status — loaded yes/no, pid, last exit code. Use to verify the daemon is running.
allowed-tools:
  - Bash(slack-sessions *)
---

Run `slack-sessions service status` and report the output.
